apiVersion: v1
kind: Service
metadata:
  name: final-service
spec:
  selector:
    app: final-app  # This MUST match the labels in your existing final-app.yaml
  ports:
    - protocol: TCP
      port: 80       # Port the Service listens on
      targetPort: 80 # Port the Nginx container listens on
  type: ClusterIP
