kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  apiServerAddress: "0.0.0.0" # Listen on all interfaces
  apiServerPort: 6443         # Fix the port so it doesn't change
nodes:
- role: control-plane
- role: worker
- role: worker
- role: worker
